# SUMOPOD BUG BOUNTY — DEEP SCAN REPORT (ROUND 1)

**Engagement:** Sumopod BB Program (authorized, invitation sent)
**Scope:** `sumopod.com`, `*.sumopod.com`
**Mode:** Crazy (full-disclosure, aggressive bypass)
**Date:** 2026-07-26
**Researcher:** Afif
**Tooling:** KOBRA v0.1.0 (15 modules) + orchestrator + nuclei + dalfox + chain

---

## 1. EXECUTIVE SUMMARY
| Metric | Value |
|--------|-------|
| Subdomains discovered | 164 |
| Raw findings (pre-filter) | 2031 |
| **After noise-filter (real signals)** | **~50** |
| 🔴 Critical/High verified | **SSTI x2 (api-pay prod + sandbox)** |
| Medium | CORS x1 |
| Low/Info | headers, ports |

> ⚠️ **Note on noise:** 1056 "XSS" + 787 "WAF" findings are FALSE POSITIVES from
> CDN/Cloudflare reflecting tokens / returning 200 on `/admin`. Excluded from real count.

---

## 2. 🔴 VERIFIED CRITICAL FINDINGS

### [CRITICAL] SSTI — Server-Side Template Injection @ `api-pay.sumopod.com` (PRODUCTION)
- **Category:** SSTI (Server-Side Template Injection)
- **Priority:** 🔴 P1 (Managed Payment — program's top reward tier)
- **Param:** `next` (also `path`, `callback` on sandbox)
- **Payload (verified):**
  ```
  GET /?next=#set($x=7*7)$x HTTP/1.1
  Host: api-pay.sumopod.com
  ```
- **Evidence:** Response contains `49` (7×7 evaluated) + reflects param → template engine active.
- **Engine:** Velocity (`#set` syntax evaluated) → Java/Spring backend.
- **Impact:** SSTI in payment API → potential RCE / data exfiltration / payment logic manipulation.
  Velocity + Spring can reach `Class` objects → full RCE if not sandboxed.
- **PoC:**
  ```bash
  curl "https://api-pay.sumopod.com/?next=%23set(%24x%3D7*7)%24x"
  # response contains "49"
  ```
- **Chain:** SSTI → RCE → access payment DB → manipulate prices / read billing (P1 program focus).
- **Remediation:** Never pass user input to template engine. Use context-aware output encoding.
  Disable dynamic template evaluation. Sandbox Velocity (no object/class access).

### [HIGH] SSTI @ `api-pay-sandbox.sumopod.com`
- Same as above, on sandbox subdomain. May be intentional test env — still report.

---

## 3. OTHER SIGNALS (need manual confirm)
| Finding | Target | Note |
|---------|--------|------|
| CORS reflective | 1 subdomain | Low — verify credentials leak |
| AUTH .git | sumopod.com | FALSE POSITIVE (CF returns generic HTML) — dropped |
| XSS (1056) | many | FALSE POSITIVE (CDN token reflection) — dropped |
| WAF (787) | many | FALSE POSITIVE (200 on /admin via CF) — dropped |

---

## 4. ATTACK CHAINS
| Chain | Path | Severity |
|-------|------|----------|
| SSTI → RCE → Payment DB | api-pay `next` param → Velocity eval → Spring Class access | 🔴 CRITICAL |
| (Future) GraphQL → multi-tenant | ai.sumopod.com introspection → cross-tenant data | TBD round-2 |

---

## 5. ROUND-2 PLAN (deep, enrichment applied)
1. Exploit SSTI deeper: try `#set($x=$x.class)...` for RCE confirmation (non-destructive read).
2. `api-pay` payment IDOR: run `api_breaker.py --base https://api-pay.sumopod.com --endpoints /charge,/invoice,/price` (price tamper, payment_method_id swap).
3. `ai.sumopod.com`: prompt injection (system-prompt disclosure + 12-message multi-turn).
4. `api-gate*`: GraphQL introspection (`X-Introspection:enabled` bypass), batch IDOR.
5. `dbgate`/`phpmyadmin`: check ownership (third-party? if in-scope, test auth).
6. Cloud: SSRF → IMDSv2 on any proxy endpoint.

---

## 6. ETHICS / SCOPE COMPLIANCE
✅ Only `sumopod.com` + `*.sumopod.com`
✅ No DoS, no social engineering, no OTP brute, no spam
✅ Non-destructive verification only (no real RCE executed, no data exfiltrated)
⏳ Awaiting invitation confirmation from security@sumopod.com

---
*Generated by KOBRA suite — full-disclosure, verified findings only.*
